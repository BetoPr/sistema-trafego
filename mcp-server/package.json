{
  "name": "@sonar/mcp-crm",
  "version": "0.1.0",
  "description": "MCP server pra Sonar CRM — acesso programatico a KPIs, campanhas, criativos, tickets, contatos via Claude Desktop/Code/Cursor.",
  "type": "module",
  "bin": {
    "sonar-mcp-crm": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
